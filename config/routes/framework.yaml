when@dev:
    _errors:
        resource: '@FrameworkBundle/Resources/config/routing/errors.xml'
        prefix: /_error
when@prod:
    _errors:
        resource: '@FrameworkBundle/Resources/config/routing/errors.xml'
        prefix: http://study-on.local:8081/_error/{statusCode}
